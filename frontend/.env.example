# Used only by the Vite development server. Production requests use /api/v1
# and are forwarded to Railway by the Vercel rewrite.
VITE_API_URL=http://localhost:5000/api/v1

# This value is public and is used only to construct Cloudinary delivery URLs.
VITE_CLOUDINARY_CLOUD_NAME=your-cloud-name
